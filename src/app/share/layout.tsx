import type { Metadata } from "next";

export const metadata: Metadata = { title: "Save to Musemint" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
